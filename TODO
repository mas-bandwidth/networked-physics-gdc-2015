DONE:

TODO:

    Fix position grid bugs!!!

    -----------------------------------------------------------------------

    Implement relative position

    Implement relative orientation

    Don't bother with separate absolute/relative indices -- just go straight to relative

    -----------------------------------------------------------------------

    Generate code to output bandwidth as CSV so I can graph it. 

    Generate code to output position delta and orientation delta
    (eg. largest component change) as a histogram so again I can graph it.

    -----------------------------------------------------------------------

    Ready to write "Snapshot Compression" article

    -----------------------------------------------------------------------
